# truthlinked-net

`truthlinked-net` provides the networking primitives used by TruthLinked nodes: post-quantum peer handshakes, encrypted transport sessions, ingress handling, discovery, and TCP socket configuration.

The crate is designed for validator and node integrations that need TruthLinked-compatible post-quantum transport.
